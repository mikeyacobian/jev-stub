# Cloudflare Workers AI — fill these in only for a live typesafe/jev call.
# Do not commit real values. Dry-run and --provider=mock work with these blank.
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
